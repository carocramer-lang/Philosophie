# Escape State of Nature

Browser-Escape-Room zur politischen Philosophie (Hobbes, Locke, Rousseau, Montesquieu, Rawls, Nussbaum).
Verzweigte Erzaehlung, Vier-Werte-System, Achievements, XP, localStorage.

## Status
Platzhalter. Lege deine bestehende Datei hier als `index.html` ab und loesche die vorhandene Platzhalter-Datei.

## Offene Punkte aus dem Review
- `finalizeRun()` in `renderOutro()` ist nicht idempotent (Gefahr Doppelzaehlung bei Re-Render).
- `dominantStat()` bricht Gleichstand still zugunsten Sicherheit.
- "Nihilist"-Achievement vermutlich unerreichbar.
- Kurzmodus ohne didaktische Reflexionskarten.

## Test ergaenzen
Nach dem Ablegen eine `test.js` anlegen, die einen Durchlauf und die vier Endings prueft.
